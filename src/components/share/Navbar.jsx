import tw from "twin.macro";

const Navbar = (props) => {

    const {login} =props;

  const Wrapper = tw.div`flex justify-around`;
  const LeftWrapper = tw.div`flex justify-between`;
  const RightWrapper = tw.div`flex justify-between`;

  let Left, Right;

  if()

  return (
    <Wrapper>
      <LeftWrapper></LeftWrapper>
      <RightWrapper></RightWrapper>
    </Wrapper>
  );
};

export default Navbar;
