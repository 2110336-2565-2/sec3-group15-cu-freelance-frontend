import React, { useReducer, useEffect } from 'react';

import { validate } from './Validator.jsx';

const styles={
    container:()=>[

    ]
}

const Input = ({label, placeholder, errorText}) =>  {
  <div>

  </div>
}

export default Input;