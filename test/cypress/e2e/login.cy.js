describe('Verify Customer Login', () => {
	beforeEach(() => {
	})
	context('Verify login with google', () => {})
	context('Verify normal login', () => {
		context('Verify login success', () => {
			it('Enter a valid Username and Password', () => {})
			it('Enter a valid  Email and Password', () => {})
		})

		context('Verify Login Failed', () => {
			context('Verify invalid username', () => {
				it('Enter a invalid username', () => {})
				it('Enter blank username', () => {})
			})
			context('Verify invalid password', () => {
				it('Enter wrong password', () => {})
				it('Enter invalid password ', () => {})
				it('Enter blank password', () => {})
			})
		})
		context('Verify can navigate to register page', () => {})
	})
})

describe('Verify Freelancer login', () => {

})
